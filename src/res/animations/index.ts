const animations = {
  playButton: require('./play-button.json'),
  error: require('./error.json'),
  offline: require('./offline.json'),
  online: require('./online.json'),
}

export default animations
